// Package appsecrets manages key/value secrets in an .env file and triggers
// an OpenClaw secrets reload after each change. Zero third-party dependencies
// (stdlib only). Designed for the single-user dashboard use case.
package appsecrets
